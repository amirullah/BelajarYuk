import 'package:flutter/material.dart';

const Color kPrimary = Color(0xFF6C63FF);   // Purple
const Color kMath = Color(0xFFFF6B6B);      // Red-coral for Math
const Color kWord = Color(0xFF4ECDC4);      // Teal for Words
const Color kScience = Color(0xFFFFE66D);   // Yellow for Science
const Color kSuccess = Color(0xFF6BCB77);   // Green for correct
const Color kError = Color(0xFFFF6B6B);     // Red for wrong
const Color kBg = Color(0xFFF8F9FF);        // Light background
const Color kAccent = Color(0xFFFF9F43);    // Orange accent
const Color kDark = Color(0xFF2D3436);      // Dark text
const Color kLight = Color(0xFFFFFFFF);     // White
