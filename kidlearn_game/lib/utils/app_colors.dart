import 'package:flutter/material.dart';

// ── Brand & netral ──
const Color kPrimary = Color(0xFF2B7FFF); // Biru brand (ceria)
const Color kPrimaryDark = Color(0xFF1B5FD9);
const Color kPrimaryLight = Color(0xFF5AA0FF);
const Color kBg = Color(0xFFEFF5FF); // Latar biru muda
const Color kSurface = Color(0xFFFFFFFF);
const Color kAccent = Color(0xFFFF9F43); // Oranye aksen
const Color kDark = Color(0xFF2D3436); // Teks gelap
const Color kLight = Color(0xFFFFFFFF);
const Color kMuted = Color(0xFF9AA0B4);

// ── Status ──
const Color kSuccess = Color(0xFF6BCB77); // Benar
const Color kError = Color(0xFFFF6B6B); // Salah
const Color kStar = Color(0xFFFFC93C); // Bintang

// ── Warna per mata pelajaran (BelajarYuk! 2.0) ──
const Color kMath = Color(0xFFFF6B6B); // Matematika — merah-oranye
const Color kEnglish = Color(0xFF4D96FF); // Bahasa Inggris — biru
const Color kIndo = Color(0xFF4ECDC4); // Bahasa Indonesia — teal
const Color kScience = Color(0xFF9B72CF); // Sains — ungu
const Color kAgama = Color(0xFF2EA36B); // Agama Islam — hijau
const Color kIps = Color(0xFFB5A72B); // IPS — hijau zaitun

// ── Alias lama (kompatibilitas v1) ──
const Color kWord = kIndo;
